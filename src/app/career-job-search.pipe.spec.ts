import { CareerJobSearchPipe } from './career-job-search.pipe';

describe('CareerJobSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CareerJobSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
